stages:
  - build
  - deploy

variables:
  REGISTRY_ENDPOINT: "cr.yandex"
  REGISTRY_ID: "crp76t66v12au4b36gbk"
  API_IMAGE: "$REGISTRY_ENDPOINT/$REGISTRY_ID/ts-api"
  UI_IMAGE: "$REGISTRY_ENDPOINT/$REGISTRY_ID/ts-ui"
  
.kaniko-build: &kaniko-build
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [""]
before_script:
    - mkdir -p /kaniko/.docker
    - AUTH=$(echo -n "json_key:$(cat "$YC_SA_KEY")" | base64 -w0 | tr -d '\n')
    - echo "{\"auths\":{\"$REGISTRY_ENDPOINT\":{\"auth\":\"$AUTH\"}}}" > /kaniko/.docker/config.json

build-api:
  <<: *kaniko-build
  script:
    - >-
      /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/Dockerfile.api"
      --destination "${API_IMAGE}:latest"
      --cache=true
      --snapshot-mode=redo
      --single-snapshot

build-ui:
  <<: *kaniko-build
  script:
    - >-
      /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/Dockerfile.ui"
      --destination "${UI_IMAGE}:latest"
      --cache=true
      --snapshot-mode=redo
      --single-snapshot


deploy:
  stage: deploy
  image:
    name: bitnamilegacy/kubectl:1.30.7-debian-12-r1
    entrypoint: [""]
  only:
    - main
  variables:
    NAMESPACE: rossmann
  before_script:
    - mkdir -p "$HOME/.kube"
    - |
      cat > "$HOME/.kube/config" <<EOF
      apiVersion: v1
      kind: Config
      clusters:
        - cluster:
            server: "${K8S_APISERVER}"
            certificate-authority-data: "${K8S_CA_CERT}"
          name: cluster
      contexts:
        - context:
            cluster: cluster
            user: ci-deployer
            namespace: ${NAMESPACE}
          name: deploy
      current-context: deploy
      users:
        - name: ci-deployer
          user:
            token: "${K8S_SA_TOKEN}"
      EOF
  script:
    - kubectl version --client 2>/dev/null || true
    - kubectl get ns ${NAMESPACE} || kubectl create ns ${NAMESPACE}
    - kubectl apply -f k8s/ -n ${NAMESPACE}
    - kubectl create secret docker-registry cr-yandex
        --docker-server=cr.yandex
        --docker-username=json_key
        --docker-password="$(cat "$YC_SA_KEY")"
        -n ${NAMESPACE} --dry-run=client -o yaml | kubectl apply -f -
    - kubectl set image deployment/ts-api ts-api=${API_IMAGE}:latest -n ${NAMESPACE} --record
    - kubectl set image deployment/ts-ui ts-ui=${UI_IMAGE}:latest -n ${NAMESPACE} --record
    - kubectl rollout status deployment/ts-api -n ${NAMESPACE} --timeout=120s
    - kubectl rollout status deployment/ts-ui -n ${NAMESPACE} --timeout=120s
