services:
  ts_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ts_api
    ports:
      - "8000:8000"
    environment:
      - PYTHONWARNINGS=ignore
    restart: unless-stopped
