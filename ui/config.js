// URL базовой API-точки. Если пусто — используется тот же origin,
// откуда открыт UI (полезно, когда UI и API за одним ingress).
// Пример для отдельного API-сервиса:
//   window.APP_CONFIG = { API_BASE_URL: "http://api.example.com" };
window.APP_CONFIG = { API_BASE_URL: "" };
